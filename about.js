const about = function(name) {
    return `<h1>This is the About Page.</h1> Hello ${name}, This Activity will teach on how to deal with a simple server and local modules in Node.js`;
}

module.exports = about;
